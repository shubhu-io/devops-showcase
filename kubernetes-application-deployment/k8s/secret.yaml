# Example secret — replace values and apply via: kubectl apply -f secret.yaml
# Do not commit real secrets. This file contains base64 of placeholder values.
apiVersion: v1
kind: Secret
metadata:
  name: demo-app-secret
  namespace: demo-app
type: Opaque
data:
  # echo -n "change-me" | base64
  api-key: Y2hhbmdlLW1l
